import React from "react";

const OAuthButtons = () => {
  return (
    <div>
      <button className="border p-2 w-full">Continue with Google</button>
      <button className="border p-2 w-full mt-2">Continue with Apple</button>
    </div>
  );
};

export default OAuthButtons;
